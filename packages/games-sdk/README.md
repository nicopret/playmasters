# games-sdk

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build games-sdk` to build the library.

## Running unit tests

Run `nx test games-sdk` to execute the unit tests via [Jest](https://jestjs.io).
