# brand

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build brand` to build the library.

## Running unit tests

Run `nx test brand` to execute the unit tests via [Jest](https://jestjs.io).
