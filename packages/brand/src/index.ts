export const colors = {
  arcadeYellow: 'var(--pm-color-arcade-yellow)',
  electricBlue: 'var(--pm-color-electric-blue)',
  crimsonRed: 'var(--pm-color-crimson-red)',
  midnightBlack: 'var(--pm-color-midnight-black)',
  neonPink: 'var(--pm-color-neon-pink)',
  gradientPurple: 'var(--pm-color-gradient-purple)',
  textPrimary: 'var(--pm-color-text-primary)',
  textSecondary: 'var(--pm-color-text-secondary)',
  background: 'var(--pm-color-background)',
  surface: 'var(--pm-color-surface)',
  accent: 'var(--pm-color-accent)',
  danger: 'var(--pm-color-danger)',
  highlight: 'var(--pm-color-highlight)',
};

export const gradients = {
  primary: 'var(--pm-gradient-primary)',
  background: 'var(--pm-gradient-background)',
  accent: 'var(--pm-gradient-accent)',
};

export const fonts = {
  heading: 'var(--pm-font-heading)',
  body: 'var(--pm-font-body)',
};

export const fontSizes = {
  xs: 'var(--pm-font-size-xs)',
  sm: 'var(--pm-font-size-sm)',
  md: 'var(--pm-font-size-md)',
  lg: 'var(--pm-font-size-lg)',
  xl: 'var(--pm-font-size-xl)',
  hero: 'var(--pm-font-size-hero)',
};

export const fontWeights = {
  regular: 'var(--pm-font-weight-regular)',
  medium: 'var(--pm-font-weight-medium)',
  bold: 'var(--pm-font-weight-bold)',
  black: 'var(--pm-font-weight-black)',
};

export const lineHeights = {
  tight: 'var(--pm-line-height-tight)',
  normal: 'var(--pm-line-height-normal)',
  relaxed: 'var(--pm-line-height-relaxed)',
};

export const space = {
  '2xs': 'var(--pm-space-2xs)',
  xs: 'var(--pm-space-xs)',
  sm: 'var(--pm-space-sm)',
  md: 'var(--pm-space-md)',
  lg: 'var(--pm-space-lg)',
  xl: 'var(--pm-space-xl)',
  '2xl': 'var(--pm-space-2xl)',
};

export const radii = {
  sm: 'var(--pm-radius-sm)',
  md: 'var(--pm-radius-md)',
  lg: 'var(--pm-radius-lg)',
  pill: 'var(--pm-radius-pill)',
};

export const shadows = {
  card: 'var(--pm-shadow-card)',
  neonPink: 'var(--pm-shadow-neon-pink)',
  neonBlue: 'var(--pm-shadow-neon-blue)',
  focusRing: 'var(--pm-shadow-focus-ring)',
};

export const z = {
  base: 'var(--pm-z-base)',
  overlay: 'var(--pm-z-overlay)',
  modal: 'var(--pm-z-modal)',
  toast: 'var(--pm-z-toast)',
  tooltip: 'var(--pm-z-tooltip)',
};

export const tokens = {
  colors,
  gradients,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  space,
  radii,
  shadows,
  z,
};

export default tokens;
