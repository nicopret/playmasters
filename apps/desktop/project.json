{
  "name": "desktop",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/desktop",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm tauri dev --config apps/desktop/src-tauri/tauri.conf.json",
        "cwd": "."
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"Skipping desktop tauri build in CI\"",
        "cwd": "."
      }
    }
  },
  "tags": []
}
